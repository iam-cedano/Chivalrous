import React, { JSX } from "react";
import { Helmet } from "react-helmet";

function Head() {
    return (
        <Helmet>
            <title>Chivalrous | Authentication</title>
        </Helmet>
    );
}

export default Head;